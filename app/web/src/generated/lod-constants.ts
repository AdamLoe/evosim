// @generated by `cargo run --bin gen-bindings` — DO NOT EDIT.
// Source of truth: src/wasm_api.rs (GRASS_LOD_BUDGET_AXIS).
//
// LOD constants shared between the sim worker (Rust) and renderer (TS).
// Single source: Rust → gen-bindings → this file → TS consumers.

/** v2.0.4 S1: clipmap budget axis in cells. The worker publishes at most
 * `GRASS_LOD_BUDGET_AXIS × GRASS_LOD_BUDGET_AXIS` grass bytes per slot.
 * Both the LOD switch threshold AND the window/texture clamp ceiling.
 * Raising it pushes downsampling later and allows a bigger window
 * before clamping. Sizes the snapshot slot (restart-scoped). */
export const GRASS_LOD_BUDGET_AXIS = 4096 as const;
