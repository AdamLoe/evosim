name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  rust:
    name: rust check + test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
      - name: fmt
        run: cargo fmt --all -- --check
      - name: clippy
        run: cargo clippy --all-targets -- -D warnings
      - name: clippy (threads)
        run: cargo clippy --all-targets --features threads -- -D warnings
      - name: test (native)
        run: cargo test --lib
      - name: test (threads)
        run: cargo test --lib --features threads
      - name: install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh
      - name: install nightly + rust-src (required for wasm atomics / build-std)
        run: |
          rustup toolchain install nightly --component rust-src
          rustup target add wasm32-unknown-unknown --toolchain nightly
      - name: wasm-pack build
        run: rustup run nightly wasm-pack build --target web --out-dir web/wasm --release --features threads

  web:
    name: web typecheck + build
    needs: rust
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
      - uses: Swatinem/rust-cache@v2
      - name: install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh
      - name: install nightly + rust-src (required for wasm atomics / build-std)
        run: |
          rustup toolchain install nightly --component rust-src
          rustup target add wasm32-unknown-unknown --toolchain nightly
      - name: wasm-pack build
        run: rustup run nightly wasm-pack build --target web --out-dir web/wasm --release --features threads
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - name: pnpm install
        working-directory: web
        run: pnpm install --frozen-lockfile
      - name: typecheck + build
        working-directory: web
        run: pnpm build
