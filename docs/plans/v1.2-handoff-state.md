# v1.2 Orchestrator Handoff State (2026-05-26)

This doc captures the v1.2 orchestrator's full state mid-pass so future-self (or a successor orchestrator instance) can pick up cleanly if context compresses.

## Current status: PR-1 COMPLETE; PR-2 P2a kicked off next

HEAD = `82afb32 test(acceptance): regen v1.2 PR-1 goldens + balance tuning for grass-world survival`
Baseline = `9700dcc` (pre-v1.2; v1.1 close).

## Pass overview

3 PRs locked by master plan (see `docs/plans/v1.2-master.md`) + amendments (`docs/plans/v1.2-amendments.md`).

- **PR-1** — toroidal world + sun deletion + grass introduction. **DONE.** Goldens regenerated to `0x74149fcf0072d91c` (sequential + threaded bit-identical).
- **PR-2** — NN expansion + nose trait + F.30 rewrite. **IN PROGRESS.** Schema will bump v2 → v3 at PR-2 close.
- **PR-3** — Eat redesign + dev panel + balance + save migration UX. Schema bumps v3 → v4 at PR-3 close.

Plus: final verifier writes `docs/research/v1.2-final-report.md`.

## PR-1 — commits landed (10 commits)

| SHA | Piece | Title |
|---|---|---|
| 695c10a | P1a-A | feat(world): add toroidal arithmetic helpers (torus.rs) and modular SpatialGrid::cell_of |
| 3c7f122 | P1a-B | refactor(world): wrap for_each_in_radius and vision raycast toroidally |
| 6732797 | P1a-C | refactor(physics): wrap repulsion, replace wall-clamp with modular wrap, wrap split-jitter |
| 84c45d0 | bookkeep | chore(decisions): append P1a-Commit-C completion entry |
| eaa455e | P1c | feat(grass): add GrassGrid module with toroidal step + bilinear sample + slider-driven r,k |
| 6045ece | P1b+P1f | refactor: delete sun mechanic, add GrassGridSnapshot, schema v1->v2 |
| 74cc913 | P1d | refactor(genome): rename Action::Photosynth -> Graze and photosynth_efficiency -> graze_efficiency |
| c994144 | P1e | feat(grass): multi-cell graze action consumes overlapping grass cells |
| 86e4fe2 | P1g | feat(render): grass density Canvas2D tint layer + wasm-api grass_buffer/grass_dim |
| 82afb32 | P1h | test(acceptance): regen v1.2 PR-1 goldens + balance tuning for grass-world survival |

## Locked decisions affecting all future PRs

From `docs/plans/v1.2-amendments.md`:

- **Constant names (locked, single source of truth):**
  - `GRASS_GRID_DIM = 240` (NOT `GRASS_DIM`)
  - `GRASS_CELL_SIZE = 2.5` (NOT `GRASS_CELL`)
  - `GRASS_CELL_COUNT = 57_600`
  - `GRASS_MAX = 1.0`
  - `GRAZE_MAX_PER_TICK = 0.4` (originally locked 0.1; lifted via §A.1 escape clause + P1h tuning)
  - `GRASS_PROPAGATION_RATE_K_DEFAULT = 0.05`
  - `GRASS_IN_CELL_GROWTH_R_DEFAULT = 0.01` (raised from 0.005 via P3e ladder lever 3)
  - `GRASS_INITIAL_SEED_COUNT_DEFAULT = 50` (raised from 8 via P3e ladder lever 1)
  - `NN_GRASS_PATCH_OFFSET = 135` (PR-2 adds)
  - `NN_GRASS_PATCH_CENTER_SLOT = 147` (PR-2 adds — `135 + 2*5 + 2`)
  - `NN_VISION_OFFSET = 9` (PR-2 P2b adds when shifting layout)
  - `NN_LAST_ACTION_OFFSET = 129` (PR-2 P2b adds)
  - `MOVE_BIAS_REROLL_INTERVAL = 20` (PR-2 P2f adds)
  - `EAT_BITE_FRACTION_DEFAULT = 0.5` (PR-3 P3a adds)
  - `SPECIES_THRESHOLD = 4.0` (reverted to v6 §H default; v1.1 had 6.0 for sun-driven fast drift)

- **Move-bias semantics: ADD (not SET)** — additive on NN-output vx/vy, preserving evolved motion. User-confirm item for closeout (revertible in one line if user prefers SET).

- **Schema-version bumps:** v1 → v2 (PR-1, DONE), v2 → v3 (PR-2 close), v3 → v4 (PR-3 close, DevSliders.eat_bite_fraction).

- **Intra-PR-2 acceptance policy:** `cargo test --release --test acceptance` MAY FAIL between commits in PR-2 only (brain shape change makes golden hash undefined until P2h regen). Per-commit gates remain: `cargo test --lib`, `cargo clippy`, `pnpm build`.

- **P3e tuning ladder for §16 acceptance** (already triggered in P1h; revisit at P2h and P3f if needed): see DECISIONS "PR-1 regen" entry for the 9-attempt gauntlet.

## PR-2 — sequence to execute

NN layout (a) full-shift locked per master §F #2:

| Slot range | Width | Contents |
|---|---|---|
| 0..9 | 9 | self-state (was 10; is_at_wall slot 5 removed by P2b) |
| 9..129 | 120 | vision passthrough (shifted from 10..130) |
| 129..135 | 6 | last_action one-hot (shifted from 130..136) |
| 135..160 | 25 | grass-patch 5×5 (NEW; P2d) |

Total `NN_INPUTS = 160`. Brain weight count: 160*24 + 24*8 = **4032** (was 3456). `INPUT_CHUNKS = 20`.

Pieces in order:

1. **P2a** — bump `NN_INPUTS` 136 → 160, recompute weight count via parametric formula, bump `INPUT_CHUNKS` 17 → 20, update compile-asserts, GUT `Brain::founder` F.30 hardwiring block leaving only uniform-random init (P2f rebuilds). Plus update `vision_layout_matches_nn_input_block` test to assert `NN_INPUTS == 10 + 120 + 6 + 24` (24 reserved zeros between P2a and P2b+P2d). Apply `#[allow(dead_code)]` to NN_FOUNDER_* constants (P2f deletes them).

2. **P2b** — delete `is_at_wall` NN input + `compute_is_at_wall` helper + `WALL_THRESHOLD_PAD` constant. Shift all slot writes 6+ down by 1. Update tests at `world/nn.rs:422,456,473` index assertions. Add `NN_VISION_OFFSET`, `NN_LAST_ACTION_OFFSET` constants for use by P2d.

3. **P2c** — add `nose_count: u8 ∈ [0,5]` genome trait. Mirror `eye_count` everywhere (Genome field, TraitMutationRates field, mutation kernel, hot mirror `g_nose_count`, snapshot_hash byte, save serde). Add `NOSE_VALID = [0,1,2,3,4,5]` constant.

4. **P2d** — 25 grass-patch NN inputs via bilinear sampling at `(cx + dx*2.5, cy + dy*2.5)` for `dx,dy ∈ {-2..2}`. Slot offset = `(dy+2)*5 + (dx+2)`. Center at slot 147. Add `NN_GRASS_PATCH_OFFSET = 135` and `NN_GRASS_PATCH_CENTER_SLOT = 147` constants. Update `build_nn_input` signature to take `grass: &GrassGrid`. Both sequential and threaded NN paths share the same builder.

5. **P2e** — nose_count = 0 gate: if `creatures.g_nose_count[i] == 0`, zero-fill `buf[135..160]`.

6. **P2f** — F.30 founder rewrite:
   - Hidden[0] = on-grass detector: `w_ih[0][147] += NN_FOUNDER_GRAZE_DETECTOR_WEIGHT (= 5.0)`.
   - Output Graze += `w_ho[3][0] += NN_FOUNDER_GRAZE_OUTPUT_WEIGHT (= 5.0)`.
   - Hidden[1] = Move baseline (symbolic): `w_ih[1][energy_frac] += NN_FOUNDER_MOVE_BASELINE (= 1.0)`; `w_ho[vx][1] += NN_FOUNDER_MOVE_BASELINE`; `w_ho[vy][1] += NN_FOUNDER_MOVE_BASELINE`.
   - Hidden[2] = energy_frac → Split: `w_ih[2][0] += NN_FOUNDER_SPLIT_FROM_ENERGY (= 1.0)`; `w_ho[Split=6][2] += NN_FOUNDER_SPLIT_FROM_ENERGY`.
   - Delete constants: `NN_FOUNDER_ENERGY_SENSOR_STRENGTH`, `NN_FOUNDER_SPLIT_ENERGY_WEIGHT`, `NN_FOUNDER_PHOTO_BIAS`.
   - Add 3 new SoA fields: `move_bias_x: Vec<f32>`, `move_bias_y: Vec<f32>`, `move_bias_reroll_at: Vec<u32>`. Push/remove/resync sites.
   - Initialize at push: `move_bias_x[idx] = rng.symm(); move_bias_y[idx] = rng.symm(); move_bias_reroll_at[idx] = tick + MOVE_BIAS_REROLL_INTERVAL`. Three call sites: `World::new`, `handle_births`, `from_save_v1`.
   - Re-roll site at start of `apply_movement_and_repulsion`: if `tick >= move_bias_reroll_at[i]` then re-roll and bump reroll_at.
   - Apply in `apply_movement_and_repulsion`: **ADD** (not SET): `vx[i] += move_bias_x[i] * MOVE_SPEED_MAX; vy[i] += move_bias_y[i] * MOVE_SPEED_MAX`. Speed cap clamp follows.
   - Add `MOVE_BIAS_REROLL_INTERVAL = 20` constant.

7. **P2g** — Schema bump v2 → v3. SaveV1 + validate_save + from_save_v1 + snapshot_hash all walk the 3 new SoA columns (12 bytes/creature) + nose_count u8 + new brain weight length (4032). Update `f26_schema_version_mismatch_errs` test literals to `found:2, expected:3`. Update `load_old_schema_returns_version_mismatch_error` literals.

8. **P2h** — Golden regen #2. Bootstrap new sequential+threaded goldens. Must pass §16 acceptance criteria (a, b, c, d). If §16 fails, apply P3e ladder; SPECIES_THRESHOLD already at 4.0; GRAZE_MAX_PER_TICK already at 0.4; may need to raise mutation_rate_multiplier or other levers.

## PR-3 — sequence to execute

1. **P3a** — Eat redesign: per-bite `transfer = eat_bite_fraction * prey.energy * (1 - prey.armor)`; predator gains `transfer * eat_efficiency`; prey loses `transfer`. Default `eat_bite_fraction = 0.5`. Add `DevSliders.eat_bite_fraction` field. Add `set_eat_bite_fraction` typed setter. Delete `EAT_DAMAGE_COEFF`, `EAT_GAIN_COEFF`. Add `EAT_BITE_FRACTION_DEFAULT = 0.5` constant. **Bump SCHEMA_VERSION 3 → 4.** Tests per p3a §8.

2. **P3b** — Dev panel UI:
   - Mount `#devpanel-box` div in `web/index.html` at `bottom: 8px; right: 8px`.
   - `~` hotkey toggles + top-bar `⚙` button.
   - `web/src/widgets/devpanel.ts` new file. `installDevPanel(world)` called from `main.ts`.
   - 7 sliders per the locked list in p3b §3.
   - Add `set_grass_propagation_rate_k`, `set_grass_in_cell_growth_r` typed setters on WorldHandle.
   - Add `WorldHandle::newWithGrassSeed(seed, count)` constructor variant.
   - CSS in `web/src/styles.css` per p3b §7.

3. **P3c** — Save migration UX modal: schema-mismatch shows "Save format updated. Starting fresh." modal → click "OK" → IDB clear → fresh world. Reuse F.26 modal pattern at `web/src/persistence/ui.ts:38`. Detection already wired (any error from `WorldHandle.fromJson` is treated as schema mismatch).

4. **P3d** — Observability counters: live grass-cell count (cells where density > 0), total grass density (sum), mean nose_count, mean eye_count. Wire into the existing perf widget or stats widget.

5. **P3e** — Balance tuning (orchestrator-owned; per F.30 precedent). After all P3 pieces land, run §16 acceptance. If fails, apply P3e ladder per amendments §A.8. SPECIES_THRESHOLD = 4.0 already; GRAZE_MAX_PER_TICK = 0.4 already.

6. **P3f** — Golden regen #3. Bootstrap new sequential+threaded goldens.

## Open questions for v1.2 closeout (final report)

Aggregate these into `docs/research/v1.2-final-report.md § Questions for the user`:

1. **Move-bias SET vs ADD.** Cross-review locked ADD (preserves NN-driven motion in evolved descendants). Brief reads "Move += baseline" literally. SET is a one-line revert if user prefers founders' Brownian-walk to override evolved motion. Where landed: ADD.

2. **GRAZE_MAX_PER_TICK = 0.4.** Amendments §A.1 explicitly authorized up to 0.25 ("Lift to 0.25 if grazers starve"). P1h tuning landed at 0.4 because 0.25 still extincts at the past-lifespan upkeep cliff. User may want to reconsider whether (a) 0.4 is too generous, (b) upkeep formula should be revisited, or (c) §A.1's 0.25 cap was a soft limit anyway.

3. **SPECIES_THRESHOLD = 4.0** (reverted from v1.1's 6.0). v6 §H spec default is 4.0. Worked in PR-1 regen. May produce more frequent speciation events than v1.1 — UX side may need toast rate-limit (which DECISIONS F.31 entry says is already in place via toast rate-limit 2s).

4. **GrassGrid.scratch zero-init on load** (P1c flagged risk #4 in p1c §11): `GrassGrid::step` writes scratch before reading. P1c's step implementation respects this (single-pass scan; reads density, writes scratch, swaps). Confirmed safe.

5. **Default `GRASS_INITIAL_SEED_COUNT_DEFAULT = 50`** (raised from brief default 8). Brief had 8; we raised to 50 to keep founders alive. Brief explicitly allows tuning via dev panel slider (range 1..100).

6. **Default `GRASS_IN_CELL_GROWTH_R_DEFAULT = 0.01`** (raised from brief default 0.005). Same reasoning as above.

7. **§16 acceptance assertions are tied to default sliders**, NOT user-tunable behavior. Tuning sliders during a sim won't re-trigger §16 — that's only at world-init defaults. Acceptable.

8. **WSL2 perf budget**: threaded acceptance exceeds 8s budget on WSL2 (9.6s in P1h regen). Test is gated behind bootstrap path so it doesn't block on WSL2 dev machines; CI passes. Existing DECISIONS line 220 entry covered this.

9. **Three schema-version bumps in one pass.** v1 → v2 → v3 → v4. Old saves from any version are unrecoverable (no migration). Aggressive but matches the brief's "Save format updated. Starting fresh." UX.

10. **Founder behavior.** With ADD move-bias + F.30 hardwiring + nose_count=0 + eye_count=0, founders move randomly (move_bias re-roll every 20 ticks) AND may pick Graze action when on grass (slot 147 detector → hidden[0] → Graze). But nose_count=0 means the detector is dormant. So founders move randomly and only graze when stumbling onto cells. This is the brief's "honestly dumb" intent.

## Process notes for the successor orchestrator

- **Use the per-piece plans** in `docs/plans/v1.2-p*.md` PLUS `docs/plans/v1.2-amendments.md` (amendments wins on conflict).
- **Use `general-purpose` subagent_type for impl agents** (Plan subagent is read-only). Sonnet model.
- **Background agents** for impl pieces; foreground only for the final regen ceremony where you may need to iterate the tuning ladder inline.
- **Conventional commit messages.** Each impl piece is ONE atomic commit.
- **No `--no-verify`, no `--amend`.** New commit on hook failure.
- **DECISIONS.md gets one-line per piece** under `## v1.2 grass mechanic`.
- **Stay in lane.** Each agent's prompt scope is bounded by master plan §B. Cross-piece concerns go via the amendments doc.
- **The §16 acceptance criteria** are tied to default constants. Tuning is allowed (P3e). The tuning happens at the regen ceremony, NOT mid-piece.

## End of handoff doc
