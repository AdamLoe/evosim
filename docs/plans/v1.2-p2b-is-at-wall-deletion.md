# v1.2 P2b — `is_at_wall` deletion plan

Per-piece plan for PR-2 piece P2b. Owns: NN-input-side wall sensor removal.

**Authoritative refs**
- Brief: `docs/plans/v1.2-grass-mechanic-brief.md`
- Master plan: `docs/plans/v1.2-master.md` §B (P2b row), §F #6 (layout-shift policy)
- DECISIONS Milestone D + S11 entries: `DECISIONS.md:32, :210, :236`

## 1. All references (grep, file:line)

Source code:
- `src/world/nn.rs:50` — doc comment opening `compute_is_at_wall`
- `src/world/nn.rs:53` — `pub(crate) fn compute_is_at_wall(creatures: &CreatureSoA, i: usize) -> f32 {`
- `src/world/nn.rs:58` — `if near < r + WALL_THRESHOLD_PAD {`
- `src/world/nn.rs:127` — threaded NN path: `let is_at_wall = compute_is_at_wall(creatures_ref, i);`
- `src/world/nn.rs:136` — threaded NN path: argument `is_at_wall,` to `pick_action_d`
- `src/world/nn.rs:168` — sequential NN path: `let is_at_wall = compute_is_at_wall(&self.creatures, i);`
- `src/world/nn.rs:179` — sequential NN path: argument `is_at_wall,` to `pick_action_d`
- `src/world/nn.rs:244` — doc comment about layout (update to new offsets)
- `src/world/nn.rs:250` — `build_nn_input` parameter: `is_at_wall_flag: f32,`
- `src/world/nn.rs:272` — `buf[5] = is_at_wall_flag; // is_at_wall`
- `src/world/nn.rs:344` — `pick_action_d` parameter: `is_at_wall_flag: f32,`
- `src/world/nn.rs:353` — `pick_action_d` body: `is_at_wall_flag,` passed into `build_nn_input`
- `src/world/nn.rs:433-434` — test assertion `inp[5], 0.0, "is_at_wall"` in `nn_input_layout_self_state_correct`
- `src/world/nn.rs:754-799` — entire test `helpers_match_legacy_inline_math` (uses `compute_is_at_wall` + `WALL_THRESHOLD_PAD`)
- `src/world/nn.rs:422, :456, :473` — call sites of `build_nn_input` in tests pass `0.0` as the `is_at_wall_flag` arg
- `src/constants.rs:111` — `pub const WALL_THRESHOLD_PAD: f32 = 5.0;`
- `src/creature.rs:76` — comment in CreatureSoA hot-mirror docstring lists `compute_is_at_wall` as a reader

**No callers outside `src/world/nn.rs`** for either `compute_is_at_wall` or the `is_at_wall_flag` flow.

## 2. Slot-shift specifics (the extras region)

Per master §F #6 the locked policy is **option (a): delete and shift down — no reserved-zero hole**.

### Current layout (HEAD, `NN_INPUTS = 136`)

| Index | Field |
|---|---|
| 0 | energy_frac |
| 1 | age_frac |
| 2 | size_norm |
| 3 | vx |
| 4 | vy |
| **5** | **is_at_wall** (DELETED) |
| 6 | cooldown_frac |
| 7 | carrion_overlap_norm |
| 8 | reserved zero |
| 9 | reserved zero |
| 10..130 | vision (120 floats) |
| 130..136 | last_action one-hot (6 floats) |

### Post-P2b layout (no grass-patch block yet — P2d adds those)

| Index | Field |
|---|---|
| 0 | energy_frac |
| 1 | age_frac |
| 2 | size_norm |
| 3 | vx |
| 4 | vy |
| **5** | **cooldown_frac** (shifted from 6) |
| **6** | **carrion_overlap_norm** (shifted from 7) |
| **7** | **reserved zero** (shifted from 8) |
| **8** | **reserved zero** (shifted from 9) |
| 9..129 | vision (120 floats) — base shifted from 10 to 9 |
| 129..135 | last_action one-hot — base shifted from 130 to 129 |
| 135..160 | grass-patch 5×5 (added by P2d) |

Total extras = 9, vision = 120, last_action = 6, grass = 25. 9 + 120 + 6 + 25 = 160. ✓

### Arithmetic cross-check with P2a + P2d

- P2a bumps `NN_INPUTS = 160`. P2b is the first commit after P2a in PR-2.
- Grass-patch indices for P2d: 5×5 patch occupies `[135..160]`; center sample (dx=0, dy=0) is at flat index `135 + 2*5 + 2 = 147`.

## 3. Sequential NN path edit (`src/world/nn.rs:168` + :179)

After P2b:

```rust
for i in lo..hi {
    let overlap =
        count_carrion_overlap(&self.creatures, &self.carrion, &self.cell_to_carrion, i);
    let prev_vx = self.creatures.vx[i];
    let prev_vy = self.creatures.vy[i];
    let (vx, vy, action) = pick_action_d(
        i,
        &mut input_buf,
        &mut hidden_buf,
        &mut output_buf,
        &self.creatures,
        &self.vision[i],
        overlap,
        prev_vx,
        prev_vy,
    );
```

## 4. Threaded NN path edit (`src/world/nn.rs:127` + :136)

Identical surgery on threaded branch (lines 121-139). Delete `compute_is_at_wall` call and `is_at_wall,` argument.

## 5. Helper deletion

`compute_is_at_wall` (`src/world/nn.rs:50-63`) has zero callers outside the two NN paths and the deleted test. Safe to delete function body + doc-comment block.

`WALL_THRESHOLD_PAD` at `src/constants.rs:111` becomes dead. Delete constant + comment.

Update hot-mirror comment at `src/creature.rs:76`:
- Before: `// build_nn_input, count_carrion_overlap, compute_is_at_wall,`
- After: `// build_nn_input, count_carrion_overlap,`

`pick_action_d` and `build_nn_input` lose `is_at_wall_flag` parameter and pass-through. In `build_nn_input`:
- `buf[5] = is_at_wall_flag; // is_at_wall` — DELETE
- `buf[6] = cooldown_frac` → becomes `buf[5]`
- `buf[7] = carrion_overlap_norm` → becomes `buf[6]`
- `buf[8], buf[9]` (reserved zero) → become `buf[7], buf[8]`
- `buf[10..130]` (vision) → becomes `buf[9..129]`
- `buf[130 + la]` (last_action) → becomes `buf[129 + la]`

(P2d will then append `buf[135..160]` for grass-patch.)

Docstring at `src/world/nn.rs:244` updates from `[0..10] / [10..130] / [130..136]` to `[0..9] / [9..129] / [129..135]`. P2d appends `/ [135..160]` later.

## 6. Test deletion

`src/world/nn.rs:754` test `helpers_match_legacy_inline_math` — **delete entire test** (lines 753-799). Note this test imports `crate::sun::SunMap` at line 756, which P1b also deletes. **Coordinate with P1b**: cleanest is for P1b to delete this test as part of sun-deletion since it `use crate::sun::SunMap`.

Other tests touching `is_at_wall`:
- `src/world/nn.rs:422` (`nn_input_layout_self_state_correct`) — drop `is_at_wall_flag` arg; delete `inp[5] == 0.0 "is_at_wall"` assertion at line 434; shift remaining index assertions down by 1.
- `src/world/nn.rs:456` (`nn_input_layout_vision_passthrough`) — drop arg; `&inp[10..130]` → `&inp[9..129]`.
- `src/world/nn.rs:473` (`nn_input_layout_last_action_onehot`) — drop arg; `inp[130 + k]` → `inp[129 + k]`.

The `vision_layout_matches_nn_input_block` test at line 395 — **P2a's plan owns this assertion update**.

## 7. Coordination notes

- **P1a (toroidal)** deletes physics-side wall behaviour. After P1a, `compute_is_at_wall` may fire wrongly near x≈0/y≈0 because those become interior toroidal points. **P2b finishes the job.**
- **P1b (sun deletion)** uses `SunMap::cell_index_for` inside `helpers_match_legacy_inline_math`. **Coordinate:** P1b should delete this test along with sun.rs.
- **P2a** lands first (bumps `NN_INPUTS = 160`).
- **P2c (nose_count)** independent — parallel-plan-safe.
- **P2d (grass-patch)** lands after P2b. Slot 135 is first grass-patch slot.
- **P2f (F.30 rewrite)** reads grass-patch center index (slot 147).

## 8. Risks

1. **`compute_is_at_wall` fires incorrectly post-P1a** with wrap-around. Between P1a-end and P2b-end the dead sensor is wrong. Goldens regen at PR-1 end and PR-2 end absorb the drift. Acceptable.
2. **Two test files in `src/world/nn.rs` use `SunMap`.** Line-754 (P2b deletes) and line-811 (kept; doesn't use SunMap directly). No collision after deletion.
3. **Off-by-one in slot-shift propagation** across `build_nn_input` and unit tests is error-prone. **Mitigation:** define `NN_VISION_OFFSET: usize = 9`, `NN_LAST_ACTION_OFFSET: usize = 129`, `NN_GRASS_PATCH_OFFSET: usize = 135` in `src/constants.rs` as part of P2b. All slice literals use named constants. Compile_assert at `src/brain.rs` validates the sum.
4. **DECISIONS log update lagging.** PR-2 closeout commit appends one-line entry.
5. **Backwards-compat of in-flight saves.** Schema version bumped by P2g at end of PR-2; modal handles user-facing reset.

### Critical Files for Implementation
- `/home/adamg/evosim/src/world/nn.rs`
- `/home/adamg/evosim/src/constants.rs`
- `/home/adamg/evosim/src/creature.rs`
