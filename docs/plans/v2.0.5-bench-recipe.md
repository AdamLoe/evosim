---
status: final
owner: bench
last_updated: 2026-06-03
okay_to_delete: false
long_lived: true
---

# Benchmark Recipe — v2.0.5

Fixed measurement scenario for v2.0.6 S9 and any future before/after perf
comparison. Every number here was confirmed against the Rust canonical source
(`app/crates/evosim/src/constants.rs`, `app/crates/evosim/src/wasm_api.rs`, `app/crates/evosim/src/world/mod.rs`) except where
explicitly flagged.

---

## 1. Seed Policy

| Field | Value | Rationale |
|---|---|---|
| `world_seed` | **42** | A fixed non-zero u32 pins biome generation (Water/Desert blob placement + species anchor positions). A zero seed causes `new_with_founder_count` to draw a random value; any non-zero value is used as-is. |
| String RNG seed | ignored / not pinned | The string RNG (xoshiro) seeds the per-tick dice. It is independent of `world_seed` and is NOT pinned because it does not affect generation-time layout. |

**Threaded trajectory note:** Rayon work-stealing makes the per-tick sim
trajectory diverge run-to-run even with a fixed `world_seed`. This is expected
and is NOT a bug. What `world_seed = 42` pins: biome grid layout, species anchor
positions (species_mode only), and initial grass/creature scatter — i.e. the
starting conditions. The trajectory from tick 1 onward varies between runs.
Measurement must therefore rely on warmup + a windowed average, not
exact-output matching.

---

## 2. Construction-Only Settings

These must be set **before** boot and require a full Restart to change. Default
values confirmed from `DevSliders::default()` in `app/crates/evosim/src/world/mod.rs` and the
constants they reference in `app/crates/evosim/src/constants.rs`.

| Setting (Rust field) | Slider name | Bench value | Rust default | Confirmed |
|---|---|---|---|---|
| `world_size` | `world_size` | **9600.0** | `WORLD_SIZE_DEFAULT = 9600.0` | yes |
| `grass_cell_size` | `grass_size` | **5.0** | `GRASS_CELL_SIZE_DEFAULT = 5.0` | yes |
| `wrap_world` | `wrap_world` | **true** (toroidal) | `WRAP_WORLD_DEFAULT = true` | yes |
| `world_seed` | `world_seed` | **42** | default 0 (random); override to 42 | yes |
| `founder_count` | `founder_count` | **32** | `STARTING_POP_DEFAULT = 32` | yes |
| `grass_initial_seed_count` | `grass_initial_seed_count` | **8000** | `GRASS_INITIAL_SEED_COUNT_DEFAULT = 8000` | yes |
| `full_grass_on_init` | `full_grass_on_init` | **false** | `FULL_GRASS_ON_INIT_DEFAULT = false` | yes |
| `grass_multisight` | `grass_multisight` | **true** | `GRASS_MULTISIGHT_DEFAULT = true` | yes |
| `species_mode` | `species_mode` | **false** (OFF) | `SPECIES_MODE_DEFAULT = false` | yes |
| `crossover_mode` | `crossover_mode` | **1.0** (FiftyFifty) | `CROSSOVER_MODE_DEFAULT = FiftyFifty` | yes |
| `starting_species_count` | `starting_species_count` | 10 (irrelevant — species_mode OFF) | `STARTING_SPECIES_COUNT_DEFAULT = 10` | yes |
| `starting_species_member_count` | `starting_species_member_count` | 10 (irrelevant — species_mode OFF) | `STARTING_SPECIES_MEMBER_COUNT_DEFAULT = 10` | yes |
| `starting_species_member_variance` | `starting_species_member_variance` | 3.0 (irrelevant — species_mode OFF) | `STARTING_SPECIES_MEMBER_VARIANCE_DEFAULT = 3.0` | yes |

**Species mode choice: OFF.** The asexual single-pool mode is simpler, more
representative of the baseline sim, and avoids the extra per-tick species-aware
creature-sector cost (16-sector vs 8-sector creature proximity). The benchmark
targets the core tick/grass/snapshot hotspots, not species mechanics. The
alternative (ON with 10 seeded species) was considered and rejected: it adds
`starting_species_count * starting_species_member_count = 100` founders plus
16-sector NN input overhead, making the baseline heavier and harder to interpret.
If a future wave explicitly benchmarks species mode, add a parallel bench run
marked `species_mode=ON` and compare against this recipe as the baseline.

**Derived construction-time dimensions** (from `WorldDims::from_world_size_with_cell_size`):
- `grass_dim` = round(9600 / 5.0) = **1920** cells/axis
- `grass_cell_count` = 1920² = **3,686,400** cells
- `hash_dim` = ceil(9600 / 10.0) = **960** cells/axis

---

## 3. Live-Tunable Settings (leave at defaults)

These apply on the next tick; set them and do not change them during measurement.
All values confirmed from `DevSliders::default()` / `app/crates/evosim/src/constants.rs`.

| Setting | Slider name | Bench value | Rust default | Confirmed |
|---|---|---|---|---|
| `energy_max` | `energy_max` | **100.0** | `energy_max: 100.0` | yes |
| `max_population` | `max_population` | **8000** | `MAX_POPULATION_DEFAULT = 8000` | yes |
| `upkeep_multiplier` | `upkeep_multiplier` | **1.0** | `upkeep_multiplier: 1.0` | yes |
| `move_cost_multiplier` | `move_cost_multiplier` | **1.0** | `move_cost_multiplier: 1.0` | yes |
| `eat_bite_fraction` | `eat_bite_fraction` | **0.5** | `EAT_BITE_FRACTION_DEFAULT = 0.5` | yes |
| `grass_energy_per_bite` | `grass_energy_per_bite` | **10.0** | `GRASS_ENERGY_PER_BITE_DEFAULT = 10.0` | yes |
| `grass_bites_per_block` | `grass_bites_per_block` | **2** | `GRASS_BITES_PER_BLOCK_DEFAULT = 2` | yes |
| `digestion_cooldown_ticks` | `digestion_cooldown` | **0** | `DIGESTION_COOLDOWN_TICKS = 0` | yes |
| `max_age` | `max_age` | **5000** | `MAX_AGE_DEFAULT = 5000` | yes |
| `split_threshold` | `split_threshold` | **99.0** | `SPLIT_THRESHOLD_DEFAULT = 99.0` | yes |
| `split_gift` | `split_gift` | **30.0** | `SPLIT_GIFT_MAX_DEFAULT = 30.0` | yes |
| `split_jitter` | `split_jitter` | **1.0** | `SPLIT_JITTER_DEFAULT = 1.0` | yes |
| `repulsion_max` | `repulsion_max` | **0.1** | `REPULSION_MAX = 0.1` | yes |
| `water_movement_penalty` | `water_movement_penalty` | **0.8** | `WATER_MOVEMENT_PENALTY_DEFAULT = 0.8` | yes |
| `desert_movement_penalty` | `desert_movement_penalty` | **0.4** | `DESERT_MOVEMENT_PENALTY_DEFAULT = 0.4` | yes |
| `trait_mutation_sigma_multiplier` | `trait_mutation_sigma_multiplier` | **0.3** | `TRAIT_MUTATION_SIGMA_MULTIPLIER_DEFAULT = 0.3` | yes |
| `mating_cooldown_ticks` | `mating_cooldown_ticks` | **200** | `MATING_COOLDOWN_TICKS_DEFAULT = 200` | yes |
| `lod_bias` | `lod_bias` | **0.0** | `lod_bias: 0.0` in `WorldHandle` | yes |

**Scatter kernel (grass_decay / grass_spread):**

| Setting | Slider name | Bench value | Rust default | Flag |
|---|---|---|---|---|
| `grass_decay_pct` | `grass_decay_pct` | **0.04** | `GRASS_DECAY_PCT_DEFAULT = 0.04` | **DRIFT FLAGGED** |
| `grass_decay_amount` | `grass_decay_amount` | **0.008** | `GRASS_DECAY_AMOUNT_DEFAULT = 0.008` | ok |
| `grass_spread_pct` | `grass_spread_pct` | **0.55** | `GRASS_SPREAD_PCT_DEFAULT = 0.55` | ok |
| `grass_spread_amount` | `grass_spread_amount` | **0.05** | `GRASS_SPREAD_AMOUNT_DEFAULT = 0.05` | ok |
| `grass_spread_ring1_pct` | `grass_spread_ring1_pct` | **0.70** | `GRASS_SPREAD_RING1_PCT_DEFAULT = 0.70` | ok |
| `grass_spread_ring2_pct` | `grass_spread_ring2_pct` | **0.22** | `GRASS_SPREAD_RING2_PCT_DEFAULT = 0.22` | ok |

**Known drift: `grass_decay_pct`.** `app/crates/evosim/src/constants.rs`
`GRASS_DECAY_PCT_DEFAULT = 0.04` (raised from 0.02 in v2.0.2 S4 tuning). The
`settings.ts` DEFAULTS blob still has `grassDecayPct: 0.02` — stale, NOT
corrected in the S4 tuning commit. The defaults-drift e2e test does NOT check
scatter-kernel sliders, so the mismatch was not caught. Rust is authoritative:
**use 0.04**. A later wave should fix `settings.ts` and add `grass_decay_pct`
to the drift test.

---

## 4. Target TPS and Measurement Methodology

| Param | Value | Source |
|---|---|---|
| `targetTPS` | **uncapped** (set to 9999 or highest available) | `settings.ts` DEFAULTS has `targetTPS: 180`; see below |
| Profiler window | **10 000 ms** (10 s) | `settings.ts` `profilerWindowMs: 10_000` |

**TPS methodology: uncapped (run as fast as possible).**

The target TPS (default 180 in `settings.ts`; `CTRL_TARGET_TPS_BITS` SAB lane
in `web/src/generated/control-sab.ts`) acts as a pacing cap — the worker sleeps
for the remainder of the 1000/targetTPS ms slice after each tick. Measuring at
the capped default (180 TPS) reveals wall-clock throughput but not
compute-time-per-tick: a tick that takes 2 ms will still sleep ~3.6 ms to hit 180
TPS, so `tick.total` in the profiler will look fast even if we have headroom to
spare.

For a **perf comparison** the relevant quantity is the time to execute one tick
at the same population and grass density, not the sleep budget. Therefore:

- Set `targetTPS = 9999` (or the largest value the slider allows) before
  starting the measurement window. This removes the sleep and lets the profiler
  report raw compute cost.
- Observe the **achieved TPS** from the snapshot header `tps_bits` field (or the
  on-screen TPS counter). This is the ground truth for "how fast is the sim
  actually running."
- Record achieved TPS as the primary perf metric; the profiler span durations are
  the diagnostic decomposition.

**Alternative considered:** measuring at capped 180 TPS to simulate "real user
experience." Rejected for before/after perf comparison because: (a) a tick that
is cut in half may still be pacing-limited at 180 TPS, making improvement
invisible; (b) the profiler's rolling-average durations are accurate at both cap
levels, but comparing achieved TPS across code changes is more meaningful when
the cap is not the bottleneck.

---

## 5. Viewport and Zoom

| Param | Value | Source |
|---|---|---|
| Canvas CSS size | **1920 × 1080 px** (or as close as the display allows) | `write_snapshot` comment: "default scale… square 1920×1080 viewport"; `wasm_api.rs` line ~752 |
| Initial zoom | **1.0** | `makeCamera` in `web/src/render.ts`: `{ zoom: 1.0, cx: worldSize / 2, cy: worldSize / 2 }` |
| Camera center | **(4800, 4800)** (world center = `worldSize / 2`) | same |
| LOD level at these settings | **mip level 0** (full resolution) | `write_snapshot` LOD formula: visible_cell_span_x = (9600 / 1.0) / 5.0 = 1920; ratio = 1920 / 4096 ≈ 0.47 < 1.0 → level 0 |

**Why viewport matters:** `write_snapshot` in `app/crates/evosim/src/wasm_api.rs` takes camera
parameters and computes the grass clipmap window every tick. The window size
(and whether it triggers mip level 0 vs level 1+) directly affects
`write_output_sab.snapshot.grass_copy` cost. At zoom = 1.0 and a 1920-wide
canvas the full 1920 × 1620 cell window is written at level 0 — the most
expensive path. Pinning zoom = 1.0 + 1920 × 1080 ensures the before/after
comparison hits the same code path.

**Resize note:** the canvas size is set by the OS/browser window. Use the
browser's DevTools device emulation to fix `1920 × 1080` if the measurement
machine has a different screen. The sim does not cap rendering to a smaller
logical size.

---

## 6. Warmup

| Param | Value | Rationale |
|---|---|---|
| Discard ticks | **first 2000 ticks** (≈ 11 s at 180 TPS; ≈ 4 s uncapped at ~500 TPS) | Grass coverage and creature population need time to reach quasi-steady state. At tick 0 the world has 32 founders and 8000 sparse grass seeds. By tick ~1000–2000 the population typically reaches its first growth plateau and the grass scatter has spread to cover most viable plains cells. |
| Population target | **steady state** — the population histogram should not be in early exponential growth; the on-screen counter should be oscillating or growing slowly before measuring. | |

**Practical warmup procedure:** boot with `world_seed = 42` and `targetTPS =
9999`. Wait until the on-screen population counter stops growing rapidly (the
"hockey stick" phase ends). For the default settings with `max_population = 8000`
this takes roughly 1000–3000 ticks. Then reset the profiler (`profile_clear`) and
start the measurement window.

---

## 7. Profiler Window

| Param | Value | Source |
|---|---|---|
| Window duration | **10 000 ms** (10 s) | `settings.ts` `profilerWindowMs: 10_000`; `wasm_api.rs` `profile_set_window_ms` clamped [500, 600_000] |
| Minimum sample count | Wait until **at least 500 ticks** fall inside the window before reading | At 50 TPS (heavy load), 500 ticks = 10 s exactly; at 500 TPS, 500 ticks = 1 s, well within the window. |

The profiler emits rolling-window means for each span. At the 10 s window and
typical 50–500 TPS uncapped, the mean will be well-averaged. Do not shrink the
window below 5000 ms for a before/after comparison; noise increases
proportionally.

---

## 8. What to Record

Capture the following from `profile_report_json()` (available at the browser
console as `world.profile_report_json()` in the sim worker, or via the perf
panel):

### Tick sub-tree (Rust profiler, `tick` root)

| Span | Notes |
|---|---|
| `tick` | Total per-tick sim work (the outer span wrapping all phases). Primary perf number. |
| `tick.grass_step` | Stochastic scatter kernel pass across the 3.7 M cell grid. Usually the dominant tick cost at default settings. |
| `tick.pyramid_refresh` | `grass.refresh_pyramid()` rebuilding the LOD mip levels. Added in v2.0.5 S1 — previously unprofiled. |
| `tick.grid.rebuild` | Spatial hash grid rebuild. |
| `tick.nn` | Parallel NN forward pass across all creatures (rayon). |
| `tick.movement` | Creature movement + biome penalty application. |
| `tick.graze` | Grass-bite resolution. |
| `tick.attack` | Attack resolution. |
| `tick.handle_births` | Birth + population-cap cull. |
| `tick.collect_deaths` | Death sweep. |
| `tick.energy_bookkeeping` | Per-tick energy drain + clamp. |
| `tick.bookkeeping_tail` | Residual bookkeeping (NN stats, genome tracking). |

### sim_worker sub-tree (worker spans routed through Rust profiler)

| Span | Notes |
|---|---|
| `sim_worker` | Total per-iter worker wall-clock (includes read + tick + write + profiler). |
| `sim_worker.tick` | Wall-clock time the worker spends on `world.step_n(1)`. Should closely track `tick` above. |
| `sim_worker.write_output_sab` | Total snapshot + other SAB write time. |
| `sim_worker.write_output_sab.snapshot` | `write_snapshot` total (all sub-phases). |
| `sim_worker.write_output_sab.snapshot.creatures` | Creature SoA pack into the snapshot buffer. |
| `sim_worker.write_output_sab.snapshot.grass_copy` | Grass clipmap window copy via `pyramid.viewport_window`. |
| `sim_worker.read_input_sab` | Control SAB read (slider/camera/pause reads). |

### Spans planned by v2.0.6 S9 (not yet wired, but anticipated)

| Span | Status | Notes |
|---|---|---|
| `sim_worker.write_output_sab.snapshot.biome` | **NOT YET WIRED** (v2.0.6 S9 adds it) | Sub-span for the per-tick biome-window mode-downsampling nested loop (`app/crates/evosim/src/wasm_api.rs` ~line 857 PERF NOTE). S9's first task is to add this span and measure whether biome recompute is the dominant snapshot cost. Record it here once added. |

### Scalar metrics to capture alongside profiler spans

| Metric | How to read | Notes |
|---|---|---|
| Achieved TPS | On-screen TPS counter or `world.tps` getter | Primary perf number; all span durations are per-tick means. |
| Population | On-screen population counter or snapshot header `pop` field | Ensures before/after comparison runs at comparable population. |
| Tick number | Snapshot header `tick` field | Confirms warmup has elapsed. |

---

## 9. Benchmark Run Procedure (Copy-Pasteable Checklist)

```
BEFORE EACH RUN:
  1. Open browser DevTools → Application → Storage → Clear site data
     (or press the "Reset settings" button in the dev panel).
     This ensures no stale localStorage overrides the DEFAULTS.

  2. In the dev panel, set construction-only settings:
       world_seed = 42
       world_size = 9600       (already default)
       grass_size = 5.0        (already default)
       wrap_world = true       (already default)
       founder_count = 32      (already default)
       grass_initial_seed_count = 8000   (already default)
       full_grass_on_init = false        (already default)
       grass_multisight = true (already default)
       species_mode = false    (already default)

  3. Set live settings:
       energy_max = 100        (already default)
       max_population = 8000   (already default)
       grass_decay_pct = 0.04  (override if localStorage shows 0.02 — known drift)
       [all other live sliders: leave at defaults, see Section 3 above]

  4. Click Apply, then Restart (seed rerolls from v2.0.5 S5, but world_seed = 42
     pins biome generation regardless of the string seed).

  5. Set targetTPS = 9999 (uncapped) via the TPS slider in the perf panel.

  6. Confirm browser viewport CSS size = 1920 × 1080 (DevTools device emulation).
     Confirm zoom = 1.0 (the camera resets on Restart).

WARMUP:
  7. Wait until the population counter is no longer in rapid exponential growth
     (typically tick 1000–3000 at these settings). Do not start measuring during
     the early growth spike.

  8. Click "Reset profiler + jank" (or call `world.profile_clear()` in the
     console) to clear accumulated samples.

  9. Wait for ≥ 500 more ticks to fall in the 10 s profiler window.

RECORD:
  10. Open the perf panel. Capture:
        - Achieved TPS (on-screen)
        - Current population
        - Current tick
        - All span mean durations from the tick and sim_worker subtrees listed
          in Section 8 above.

  11. Copy the full `profile_report_json()` output and paste into the
      before/after comparison table.
```

---

## 10. Limitations and Pinnable/Non-Pinnable Knobs

| Knob | Pinnable? | Notes |
|---|---|---|
| Biome layout | YES (via `world_seed = 42`) | Deterministic from `world_seed` via SplitMix64 in `app/crates/evosim/src/world/biome.rs`. |
| Species anchor positions | YES (via `world_seed = 42`, species_mode only) | Deterministic from `world_seed ^ SEEDING_PRNG_SALT`. Moot here (species_mode OFF). |
| Grass scatter initial cell positions | YES (via `world_seed`) | Grass boot seeding is deterministic from `world_seed`. |
| Per-tick sim trajectory | NO | Rayon work-stealing makes the creature decisions diverge run-to-run even with a fixed seed. This is expected and documented in v2.0.4 architecture decisions. The recipe compensates with warmup + windowed averaging. |
| Canvas physical pixel count | PARTIAL | CSS pixels are fixed at 1920 × 1080; physical pixels depend on `devicePixelRatio` (1.0 on a standard display, 2.0 on retina). The render layer uses the physical pixel canvas; the sim `write_snapshot` path (the perf target) uses the CSS viewport dimensions for its LOD computation — so DPR does not affect snapshot cost. |
| Rayon thread count | NOT DIRECTLY PINNABLE from the UI | Determined by browser's SharedArrayBuffer thread pool size. Typically 8 logical cores. The bench recipe does not pin core count; note the machine's core count in the before/after table. |
| NN topology | YES (via Settings → NN tab, default `32→48→24→5`) | The recipe uses the legacy default topology. If a future wave changes the topology, record it in the comparison table. |

---

## 11. Before/After Comparison Table Template

Copy this and fill in actual numbers.

| Metric | Baseline (sha: ______) | After change (sha: ______) | Delta |
|---|---|---|---|
| Achieved TPS | | | |
| Population | | | |
| Tick # at measurement | | | |
| `tick` mean (µs) | | | |
| `tick.grass_step` mean (µs) | | | |
| `tick.pyramid_refresh` mean (µs) | | | |
| `tick.grid.rebuild` mean (µs) | | | |
| `tick.nn` mean (µs) | | | |
| `tick.movement` mean (µs) | | | |
| `tick.graze` mean (µs) | | | |
| `tick.attack` mean (µs) | | | |
| `tick.handle_births` mean (µs) | | | |
| `sim_worker` mean (µs) | | | |
| `sim_worker.tick` mean (µs) | | | |
| `sim_worker.write_output_sab` mean (µs) | | | |
| `sim_worker.write_output_sab.snapshot` mean (µs) | | | |
| `sim_worker.write_output_sab.snapshot.creatures` mean (µs) | | | |
| `sim_worker.write_output_sab.snapshot.grass_copy` mean (µs) | | | |
| `sim_worker.write_output_sab.snapshot.biome` mean (µs) | (span not yet wired) | | |
| Machine core count | | | |
