# v1.6 Orchestrator Prompt (paste into a fresh chat)

Copy everything between the `---BEGIN---` and `---END---` markers below into a fresh Claude Code conversation rooted at `/home/adamg/evosim`.

---BEGIN---

You are orchestrating the v1.6 pass on evosim: a Rust→WASM browser evolution simulator. The mission, mission review, plan, plan review, and implementer guide are all already written. Your job is to ship the plan via parallel implementer agents in worktrees, reconciling between waves, and verifying at the end.

This is **not** a planning task. The plan is locked. Do not re-litigate decisions; do not re-review the review. Just ship it.

## Required reading (read all in order, before spawning anything)

1. `/home/adamg/evosim/docs/plans/v1.6-mission.md` — locked mission. Read in full.
2. `/home/adamg/evosim/docs/plans/v1.6-mission-review.md` — review synthesis. Skim for context; the locked decisions in §3 below capture what shipped.
3. `/home/adamg/evosim/docs/plans/v1.6-plan.md` — **the authoritative implementation plan**. Read in full. The per-step prompts in §3 are the source of truth for each implementer agent.
4. `/home/adamg/evosim/docs/plans/v1.6-plan-review.md` — review of the plan. All 5 Critical and 10 Important items are already folded into the plan's locked decisions and per-step prompts. Read for context, not as a task list.
5. `/home/adamg/evosim/docs/plans/v1.6-implementer-guide.md` — **the operational runbook**. Read in full. Wave sequencing, gates, reconciliation, failure modes, gotchas.
6. `/home/adamg/evosim/docs/dev-server-prompt.md` — dev server (port 47821), COOP/COEP, wasm-pack incantation.
7. `/home/adamg/evosim/DECISIONS.md` §"v1.5" — what shipped last time. You'll append `## v1.6` at the end of your work.

Briefly check the code surfaces v1.6 will touch (don't re-summarize; just orient):

- `src/wasm_api.rs` — WorldHandle surface (stride 6→8 in A1; deletions in C).
- `src/world/mod.rs`, `src/world/tick.rs` — World::step orchestration.
- `src/constants.rs` — MAX_POP_FOR_SAB lands here in A1.
- `web/src/main.ts` — becomes thin in B.
- `web/src/render-gl.ts` — tolerant stride guard in A1; structural in B; JS-side cull+pack in C.
- `web/vite.config.ts` — `worker: { format: "es" }` already set; sim worker piggybacks.
- `web/wasm/snippets/wasm-bindgen-rayon-*/src/workerHelpers.js` — precedent for nested worker spawning.

## Baseline

Main is at `e29c94d docs: make threaded wasm build the unambiguous default`. Repo is green: `cargo test --lib`, `--features threads`, `cargo clippy -- -D warnings` (both feature sets), `pnpm typecheck`, `pnpm build`.

There may be uncommitted modifications on main from prior dev (`git status`). Read them first; decide stash / commit / revert before starting Wave A. The plan assumes a clean main.

## The five Critical fixes from the plan review (already folded — verify, don't re-debate)

These bit the planner and would have bit the implementer. The current plan has them locked. Surface them to each Wave's implementer agent so they don't accidentally regress:

1. **`Atomics.waitAsync` + `await`** in the Wave D sim loop (NOT `Atomics.wait` — would dark-hole every message).
2. **`worker.snapshot.write` profile span is TS-side** in Wave C (Rust-side would orphan it at root level).
3. **No `cross_origin_isolated()` Rust export** — JS reads the global directly on both sides.
4. **Tolerant stride guard** `if (stride !== 6 && stride !== 8)` in Wave A1 (renderer must boot in the A1→B window).
5. **Worker runs one tick + writes one snapshot before `boot_ready`** (Stage 1 + Stage 2) so main's first frame is never `pop=0`.

If you find an implementer agent about to violate one of these, stop it. These are non-negotiable.

## Orchestration approach (5 phases)

### Phase 0 — Pre-flight (5 minutes)

```bash
git status                            # any uncommitted dev?
git log --oneline -3                  # confirm baseline e29c94d
git -C /home/adamg/evosim worktree list    # any stale worktrees? prune if safe
```

If `git status` shows uncommitted files in src/ or web/ that look v1.6-related, **stop and surface them to the user**. Otherwise, stash or commit before Wave A.

### Phase 1 — Wave A (parallel, ~30–60 min)

Spawn **two** implementer agents in parallel, both `isolation: "worktree"`, both `run_in_background: true`:

- **A1** — Rust snapshot infrastructure. Prompt: copy `v1.6-plan.md` §"Step A1" verbatim into the agent's prompt + the implementer-guide §"Wave A — Agent A1" gotchas. Branch: `worktree-a1-snapshot-infra`.
- **A2** — sim-bridge.ts types. Prompt: copy `v1.6-plan.md` §"Step A2" verbatim + the implementer-guide §"Wave A — Agent A2" gotchas. Branch: `worktree-a2-sim-bridge-types`.

Both lands a single atomic conventional commit. Both run the per-commit gate suite (implementer-guide §"Per-commit gate suite"). A2 may skip the `wasm-pack` rebuild step.

Wait for both to complete (you'll be notified). Reconcile inline: merge A1 first, then A2. Run the full gate suite on merged main. Boot the dev server and confirm renderer still works under the tolerant stride guard.

### Phase 2 — Wave B (serial, ~1.5 days)

This is the biggest commit in v1.6. Spawn **one** implementer agent, worktree-isolated, **foreground** (`run_in_background: false`). Branch: `worktree-b-stage1-cutover`.

Prompt: copy `v1.6-plan.md` §"Step B" verbatim + implementer-guide §"Wave B" (all 11 gotchas + the acceptance gate list).

When the agent reports back, reconcile to main and **run the Wave B acceptance gate** (implementer-guide §"Wave B acceptance gate" B-a through B-h). Do not proceed to Wave C if any of B-a / B-b / B-c / B-g fail.

**Hard rule:** if pop > 1500 regresses FPS, that's expected (structured-clone cost) and Wave C closes it. The locked acceptance ceiling is pop ≤ 1500.

### Phase 3 — Wave C (serial, ~1 day)

One implementer agent. Worktree, foreground. Branch: `worktree-c-sab-snapshots`.

Prompt: `v1.6-plan.md` §"Step C" + implementer-guide §"Wave C".

After reconciliation, run the Wave C acceptance gate. Verify the long deletion list landed (no `creatures_buffer` / `pack_render_buffer` / `stats_sample` / per-typed slider setters referenced anywhere in `web/src/`).

### Phase 4 — Wave D (serial, ~0.5 day)

One implementer agent. Worktree, foreground. Branch: `worktree-d-stage3-polish`.

Prompt: `v1.6-plan.md` §"Step D" + implementer-guide §"Wave D" (with **emphasis on `Atomics.waitAsync` + `await`** — this is where the v1.6 plan review found its single critical bug).

After reconciliation, run the Wave D acceptance gate.

### Phase 5 — Wave E (verify, ~30 min)

No worktree, no implementer agent. Orchestrator runs the Wave E checklist directly (implementer-guide §"Wave E"). Items (a–j). If any of (a–c) fail, halt — that's the v1.5 silent-single-thread footgun.

Boot the dev server, drive it manually (or via headless probe), capture `profile_report_json()` at pop = 1500.

### Phase 6 — Cleanup + docs (~30 min)

After Wave E ships clean:

- Append `## v1.6 (YYYY-MM-DD)` section to `DECISIONS.md` with the per-commit SHAs and a 1-paragraph summary of what shipped.
- Update `README.md` if architectural diagram needs refresh (sim worker vs main thread split).
- Optionally: spawn an audit agent to find leftover stubs, dead `#[allow(dead_code)]`, stale `// TODO: sim-worker` comments.

## Constraints

- Conventional commits only.
- Per-commit gate suite (implementer-guide §"Per-commit gate suite") must pass.
- No `--no-verify`, no `--amend` on hook failure — create a new commit.
- The threaded wasm build is the only acceptable build. Verify with `grep -c initThreadPool web/wasm/evosim.js` returns 2 and `grep -F 'shared:true' web/wasm/evosim.js` returns 1. If either check fails, the bundle silently single-threaded — root-cause it.
- `web/wasm/` is gitignored. Any Rust signature change requires the threaded wasm-pack rebuild before `pnpm typecheck`.
- Never `git add -A` or `git add .` — use `git add -- <explicit files>`. The repo's `.gitignore` skips `.claude/worktrees/` but defense in depth matters; v1.5 hit this footgun.
- Stage 1 + Stage 2 (Waves B + C) cannot be bundled into one merge. Staging boundary is non-negotiable so the bisect surface stays narrow.
- No Vitest harness, no inspector streaming, no GPU grass, no save-load. Out of scope (`v1.6-mission.md` §"Non-goals (v1.7+)").

## Style + tone

- Match the user's terseness. Brief status updates; one-sentence end-of-turn summaries.
- When delegating to agents, give them self-contained prompts that include the relevant plan section + implementer-guide gotchas. Don't make them re-derive design from the mission.
- Reconcile inline when the conflict is trivial; spawn a dedicated reconciliation agent only for Wave B (the heavy one).
- If a gate fails, fix it in the current wave with a new commit. Don't paper over with `#[allow(...)]` unless the dead-code lifetime is bounded to the next wave.

## Specific guidance — the 17 locked decisions

These are in `v1.6-plan.md` §"Locked decisions". Don't ship anything that violates them. The most load-bearing:

1. **Path A** — SAB external to wasm linear memory; single wasm instance in sim worker; main holds no wasm.
2. **JS-side frustum cull** — `pack_render_buffer` is deleted in Wave C; renderer culls + packs in JS.
3. **Snapshot stride = 8** with `id_lo` / `id_hi` packed as `f32::from_bits(u32)` for clean Rust↔TS round-trip.
4. **`step_n(batch)`** with the fractional-budget accumulator (lifted verbatim from `main.ts:222-232`).
5. **`Atomics.waitAsync` + `await`** in the sim loop. NOT `Atomics.wait` — that blocks the event loop.
6. **Snapshot stats block** carries `tick`, `pop`, `world_ended`, `tps` (as `f32::to_bits`), `jank_count`.
7. **`write_snapshot_to(...)` Rust API** is the only Rust → SAB write path.
8. **Message-protocol table** in `v1.6-plan.md` §"Sim-bridge message protocol" is canonical.
9. **Stage 1 acceptance is correctness** (pop ≤ 1500), not perf.
10. **Time budget: 3–4 days** (plan-review's correction to the plan's 2.5–3).
11. **Worker-stats + perf-report polling** are 750 ms / 1 s request-reply.
12. **`crossOriginIsolated` check on both main and worker**, JS-side (no Rust export).
13. **Slider drain ordering**: worker drains queue at the **top of each loop iteration**, before `step_n` and snapshot-write.
14. **Restart blip target 500 ms, realistic ~600 ms**.
15. **First-paint handshake**: worker runs one tick + writes one snapshot before posting `boot_ready`.
16. **Stage 1 acceptance ceiling pop ≤ 1500**; no B+C bundling.
17. **Tolerant stride guard `6 || 8`** in A1; tightens to `!== 8` in B.

If something seems ambiguous, check `v1.6-plan.md` §4 "Ambiguities surfaced" — it covers 15 follow-up resolutions.

## Start with

Run `git status` and `git log --oneline -3` to confirm baseline. Read the 5 docs listed under "Required reading" top-to-bottom. Then spawn Wave A's two agents in parallel.

Don't ask the user "should I start?" — start. The user said go.

---END---

## Notes for the user (you, reading this in the current chat)

This prompt assumes the new chat has access to `/home/adamg/evosim` and the docs/plans/ files referenced above. Drop it into a fresh `claude` invocation in this directory.

### Things to verify before pasting

- **Baseline SHA.** Current HEAD is `e29c94d`. If you've committed since this chat, update the prompt's "Baseline" section.
- **Uncommitted state on main.** `git status` shows ~10 modified files (carried over from this planning session — e.g., `docs/plans/v1.3-master.md`, `src/grass.rs`, etc.). The new chat will surface them in Phase 0; if any are v1.6-relevant, decide stash / commit / revert before pasting.
- **No `v1.6-orchestrator-prompt.md` already running.** This prompt assumes the v1.6 work hasn't started yet. If you've already landed some waves, edit the prompt's "Orchestration approach" section to start from the wave that's next.
- **`docs/plans/v1.6-plan.md` already reflects every fix from the plan review.** You don't need to re-link the plan-review — the orchestrator reads it as context only.

### Cleanup before fresh chat (optional)

If there are stale worktrees from earlier sessions:
```bash
git worktree prune
git branch | grep '^[[:space:]]*worktree-' | xargs -r git branch -D
rm -rf .claude/worktrees
```
Only works if Claude isn't currently holding locks on them.

### Expected outputs of the fresh chat

- Wave A: 2 commits (A1 + A2) on main.
- Wave B: 1 commit (the big Stage 1 cutover).
- Wave C: 1 commit (Stage 2 SAB swap).
- Wave D: 1 commit (Stage 3 polish).
- Possible `fix(runtime):` commits from Wave E.
- Updated `DECISIONS.md` with `## v1.6` section.
- Possibly updated `README.md`.
- Total: 5–7 commits on main, all green gates.

### Time horizon

3–4 working days of orchestrator + agent wall time. Wave B is the long pole (1.5 days). Wave E is fast (30 min) but is the gate that decides whether the user is on the hook for follow-up.

### What to do if the orchestrator gets stuck

If the new chat surfaces a design question that isn't covered by the locked decisions or `v1.6-plan.md` §4 ambiguities, answer it inline and tell it to update §4. If the question reveals a real gap in the mission/plan, edit the source doc (mission or plan) and re-point the orchestrator. Don't let it improvise architecture.
