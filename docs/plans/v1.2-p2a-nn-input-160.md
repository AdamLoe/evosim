# v1.2 P2a — NN_INPUTS 136 → 160 + brain weight shape

Scope: bump NN input dimension 136 → 160, recompute weight count 3456 → 4032, update SIMD chunk arithmetic, gut `Brain::founder`'s F.30 photosynth-bias block leaving uniform-random init, leaving F.30 rebuild to P2f. Does NOT add the 25 grass-patch inputs (P2d), does NOT delete `is_at_wall` (P2b), does NOT touch `nose_count` (P2c).

## 1. Constants change

### `src/constants.rs:63`

```rust
pub const NN_INPUTS: usize = 160; // see v1.2 grass mechanic
```

### `src/constants.rs:66`

`NN_WEIGHT_COUNT` formula parametric. Expands from 3456 to **4032** = 160*24 + 24*8 = 3840 + 192. **No source edit needed.**

### `src/constants.rs:77-79` — NOT touched here

Three founder hardwiring constants `NN_FOUNDER_ENERGY_SENSOR_STRENGTH`, `NN_FOUNDER_SPLIT_ENERGY_WEIGHT`, `NN_FOUNDER_PHOTO_BIAS` become dead-code after gut. Apply `#[allow(dead_code)]` interim; P2f deletes.

### `src/brain.rs:15` — compile-time assertion

```rust
const _: () = assert!(NN_INPUTS == 160);
```

### `src/brain.rs:29` — INPUT_CHUNKS

```rust
const INPUT_CHUNKS: usize = NN_INPUTS / 8; // 160/8 = 20
```

Parametric; only comment changes.

### `src/brain.rs:35` and `:1` — doc comments

Update to "160 inputs" and "4032 weights".

### `src/world/nn.rs:237, :244, :333, :393-399` — doc comments

`build_nn_input` doc updates to "160-float NN input vector". Layout doc on line 244 rewritten by P2b/P2d.

Test `vision_layout_matches_nn_input_block` at L394-400:
```rust
// Under P2a alone (before P2b/P2d): 10 self-state + 120 vision + 6 last_action + 24 reserved zero = 160
assert_eq!(NN_INPUTS, 10 + VISION_LEN + 6 + 24);
```
P2b and P2d rewrite this test again.

## 2. Brain::founder gut

Current `src/brain.rs:40-81`:
- L40-44: uniform-random init — **KEEP**
- L45-58: F.30 doc comment — **DELETE**
- L60-75: F.30 hardwiring body — **DELETE**
- L77-80: Self return — **KEEP**

Result: 6-line function.

```rust
pub fn founder(rng: &mut SimRng) -> Self {
    let mut weights = vec![0.0_f32; NN_WEIGHT_COUNT];
    for w in &mut weights {
        *w = rng.uniform(-NN_INIT_RANGE, NN_INIT_RANGE);
    }
    Self {
        weights,
        nn_mutation_rate: NN_MUT_RATE_DEFAULT,
    }
}
```

**RNG draw consequence:** `rng.uniform` called 4032 times (was 3456). World's `SimRng` advances by +576 extra calls at init. Golden regen at PR-2 end absorbs. Both sequential and threaded paths see same advancement (founder runs once on single thread at construction).

## 3. SIMD alignment audit

- `NN_INPUTS = 160`. 160 % 8 == 0 ✓. `INPUT_CHUNKS = 20`.
- `NN_HIDDEN = 24` (unchanged). 24 % 8 == 0 ✓. `HIDDEN_CHUNKS = 3` (unchanged).
- `NN_OUTPUTS = 8` (unchanged).

SIMD chunk loops in `Brain::forward` reference named constants. No edit to loop bounds.

Stack scratch per forward call: `(160 + 24 + 8) * 4 = 768` bytes (was 672). Negligible.

## 4. Compile-time assertions

`src/brain.rs:15-26`:
- L15: assert NN_INPUTS == 160 — EDIT
- L16-17: unchanged
- L18-21: assert multiple of 8 — KEEP (160 ok)
- L22-25: NN_HIDDEN multiple of 8 — KEEP
- L26: weight count formula — KEEP (parametric)

## 5. Bit-identity invariant

Audit of `src/world/nn.rs`:
- L109, L110, L111 (threaded), L162-164 (sequential), L253, L255, L338-340 (signatures): all reference `NN_INPUTS` constant. ✓

Both paths reference the constant, not literal 136. Recompiling propagates atomically.

Threaded `par_chunks_mut` partitions creatures, not slots. Closure builds `input_buf` via `build_nn_input` exactly as sequential.

**P2a-alone scenario:** `build_nn_input` writes only to slots 0..7, 10..130, 130..136. Slots 8, 9, 136..160 remain zero-init. Identity-preserving — new trailing slots' weights are random-init nonzero but multiply zeros.

S23 test `threaded_nn_in_place_writes_match_sequential` at `src/world/nn.rs:810-868` continues to pass.

## 6. Tests to update

### `src/brain.rs:223` — diffs comment

`≈ 0.1 × 3456 ≈ 346` → `≈ 0.1 × 4032 ≈ 403`. Bound (100, 800) still encompasses. Comment-only.

### `src/brain.rs:319, 344` — pre-activation magnitude comments

`-1 * 136 = -136` → `-1 * 160 = -160`. Comment-only.

### `src/world/nn.rs:393-400` — vision_layout_matches_nn_input_block

Update assertion as in §1.

### `src/world/nn.rs:406-442, 445-462, 465-484` — index tests

None shift under P2a (P2b owns the shift cascade). No edit needed for P2a-alone.

### Other tests in `src/brain.rs:189-396`

- `founder_weight_layout_correct` (L193): "any nonzero" now relies on RNG luck (effectively zero chance of all-zeros across 4032 weights). No edit; note risk.
- `child_mutation_*`: parametric; pass.
- `forward_pass_*`: parametric; pass.

### `tests/`

`grep -rn "NN_INPUTS|136\b|3456" tests/` — no hits. Acceptance harness uses golden hashes, not weight counts.

### `src/save.rs:259, 293` — validate_save

`NN_WEIGHT_COUNT` referenced via constant import; auto-propagates. P2g handles schema bump.

### `src/snapshot_hash.rs:127`

Parametric; no edit needed.

## 7. Save-schema interaction

P2a changes on-disk brain-weight length 3456 → 4032 per creature. `validate_save` at L293 rejects mismatched lengths with `LoadError::StructuralError`. P2g (master §B PR-2) owns explicit `SCHEMA_VERSION` bump 2 → 3.

Any old save between PR-1 and PR-3 fails `validate_save` with length mismatch → `SchemaVersionMismatch` path → P3c modal.

## 8. Input-slot-allocation lock (layout (a) full-shift)

End-state after PR-2:

| Slot range | Content | Width |
|---|---|---|
| 0 | energy_frac | 1 |
| 1 | age_frac | 1 |
| 2 | size_norm | 1 |
| 3 | vx_norm | 1 |
| 4 | vy_norm | 1 |
| 5 | cooldown_frac (was is_at_wall) | 1 |
| 6 | carrion_overlap_norm | 1 |
| 7, 8 | reserved zero | 2 |
| 9..129 | vision passthrough | 120 |
| 129..135 | last_action one-hot | 6 |
| 135..160 | grass_patch 5×5 | 25 |

**Critical caveat for P2a alone:** P2a does NOT shift layout. It only extends buffer length to 160 and leaves slots 136..160 zero-init. The shift table above is END STATE after P2b + P2d.

**Risk:** if P2a accidentally introduces nonzero init at slots 136..160 in `build_nn_input`, P2d conflicts. **Mitigation:** P2a's `build_nn_input` continues writing only to slots 0..8, 10..130, 130..136. New slots remain zero-init default.

## 9. Risks

### R1: "any nonzero" weight assertion may flake on RNG seed
With NN_INIT_RANGE=0.3 across 4032 calls, probability of all-zero is effectively 0. No mitigation needed.

### R2: Hidden activations differ from existing golden on tick 1
EXPECTED and HANDLED — regen #2 at PR-2 end absorbs. **Intra-PR-2 commits will break §16 acceptance** until regen.

**Surface to orchestrator: clarify intra-PR-2 acceptance policy.** Likely answer: acceptance gate only at PR boundary, not per-commit within PR-2.

### R3: Pre-existing test asserts specific input index value
P2a-stage: no risk. `is_at_wall` slot 5 unchanged; P2b owns shift cascade.

### R4: `NN_FOUNDER_*` constants become dead-code
Apply `#[allow(dead_code)]` interim. P2f deletes.

### R5: Documentation drift
`docs/architecture.md:19` cites 136 inputs / 17 chunks. **Out of P2a scope** — include in PR-2 closeout commit or final v1.2 docs sweep.

### R6: Brain weight transpose (perf-hot-loop)
v1.2 brief excludes "Brain weight transpose / SIMD reshape". No risk.

### R7: `Brain::child_from` mutation rate
Parametric — auto-scales. Geometric-skip at p=0.02: expected mutations per child ~80 (was ~69, +17%). Marginal pressure increase. May want re-tune `NN_MUT_RATE_DEFAULT` in P3e. **Advisory.**

### Critical Files for Implementation
- /home/adamg/evosim/src/brain.rs
- /home/adamg/evosim/src/constants.rs
- /home/adamg/evosim/src/world/nn.rs
