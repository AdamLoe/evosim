# v1.3 D6 — Delete snapshot_hash + acceptance test + S39 threaded-equivalence

## 1. Scope summary

D6 strips the entire determinism-verification floor from evosim:
- Delete `src/snapshot_hash.rs` (entire file, 6 self-tests).
- Delete `tests/acceptance.rs` (5 test functions).
- Delete both golden files.
- Delete `EVOSIM_WRITE_GOLDEN` bootstrap mechanism (lives inside acceptance.rs).
- Delete `World::force_sequential_nn` test-only knob and its 3 test consumers in `src/world/nn.rs`.
- Delete the 2 CI steps that run acceptance gates in `.github/workflows/ci.yml`.
- Drop `ACCEPTANCE_TICKS` / `ACCEPTANCE_SEED` constants from `src/constants.rs`.
- Update `src/lib.rs` to drop `pub mod snapshot_hash;`.
- Strip stale "S39 / snapshot_hash / golden" doc-comments throughout `src/grass.rs`, `src/creature.rs`, `src/world/mod.rs`, `src/world/nn.rs`, `src/rng.rs`.

Per Q5: `cargo test --lib` (default + threads) is the ONLY remaining test gate.

## 2. Files affected

### Deleted (5)
- `/home/adamg/evosim/src/snapshot_hash.rs` (305 lines)
- `/home/adamg/evosim/tests/acceptance.rs` (307 lines)
- `/home/adamg/evosim/tests/golden_snapshot_t10000.txt`
- `/home/adamg/evosim/tests/golden_snapshot_t10000_threaded.txt`
- (`tests/` directory then empty)

### Modified — Rust

- `src/lib.rs` — drop `pub mod snapshot_hash;`. Coordinate `pub mod save`/`pub mod world` narrowing with D1.
- `src/world/mod.rs` — delete `force_sequential_nn: bool` field + initializer + struct-comment. Strip stale snapshot_hash comments.
- `src/world/nn.rs` — delete threaded early-return block. The threaded body itself stays (D6 deletes only the bypass knob, not the parallelism). Delete `threaded_nn_in_place_writes_match_sequential` and `nn_input_patch_threaded_matches_sequential` tests. Keep `chunk_partition_invariants_and_vision_agreement` (invariant test, not threaded-equivalence).
- `src/world/save_v1.rs` — drop `force_sequential_nn: false` initializer (D1 deletes file).
- `src/constants.rs` — delete `ACCEPTANCE_TICKS`, `ACCEPTANCE_SEED`, the acceptance section header.
- `src/rng.rs` — update `state()` doc-comment to drop snapshot_hash reference. Gate `state()` with `#[cfg(test)]` so production wasm bundle ships zero-orphan code.
- `src/grass.rs`, `src/creature.rs`, `src/species.rs` — strip stale doc-comment refs only.

### Modified — CI / scripts

- `.github/workflows/ci.yml` — delete the two acceptance test steps. Add (or confirm) `cargo clippy --all-targets --features threads -- -D warnings` + `cargo test --lib --features threads` steps.

### Modified — docs (light)

- `README.md` — remove line referencing acceptance gate.
- `BUILD-REPORT.md` — strike F.30 / acceptance pass claim.
- `DECISIONS.md` — append v1.3 D6 entry.

## 3. Structs / types / constants / fields removed

- Function `evosim::snapshot_hash::snapshot_hash(w: &World) -> u64`.
- Private helpers `hash_genome`, `write_f32`.
- Module `evosim::snapshot_hash`.
- Field `World::force_sequential_nn: bool`.
- Constants `ACCEPTANCE_TICKS`, `ACCEPTANCE_SEED`.

NOT removed:
- `twox-hash` crate (used by `rng.rs` + future M5).
- `SimRng::state()` (gated `#[cfg(test)]`).
- `SpeciesRegistry::next_id` (D10 deletes).

## 4. Tests removed

8 tests:
- `acceptance_t10000`, `profile_does_not_change_hash`, `save_load_step_preserves_determinism`, `save_load_hash_equal_immediately_after_load`, `acceptance_t10000_threaded`, `acceptance_threaded_actually_matches_sequential_t10000` (tests/acceptance.rs)
- 6 tests in `snapshot_hash.rs::tests`
- 2 tests in `world/nn.rs`: `threaded_nn_in_place_writes_match_sequential`, `nn_input_patch_threaded_matches_sequential`

Retained:
- `chunk_partition_invariants_and_vision_agreement` (invariant, not equivalence).
- Per-module unit tests in nn.rs, grass.rs, brain.rs.

## 5. Cross-piece dependencies

### D1 (save deletion)
`tests/acceptance.rs::save_load_*` tests import `evosim::save::SaveV1`. **Recommend: order D6 BEFORE D1.** D6 deletes the save-using acceptance tests by deleting the whole file.

### D7 (torus revert)
Independent. Torus revert would have changed the golden hash; D6 deletes the gate first, unblocking D7.

### Production callers
Zero production callers of `snapshot_hash::*` — only `tests/acceptance.rs` + stale comments.

## 6. Risks

### A. Removing the determinism floor
- §16 acceptance test was the single 10k-tick verification point.
- S23/S39 threaded-equivalence at full tick loop is no longer regression-tested.
- User signed off (Q5). Document the trade-off in DECISIONS.md.

### B. cargo test --lib content shrinks
- Test count drops by ~8.
- Retained tests still catch piecewise invariants.

### C. Stale doc/comment refs
- Strip only factually-wrong comments. Leave historical "S7-added" markers.

### D. wasm_api.rs `pub mod world` narrowing
- Conservative: leave `pub mod world` as-is to avoid breaking wasm bindings.
- Only delete `pub mod snapshot_hash;`.

### E. Atomic deletion required
- `force_sequential_nn` is referenced by 2 tests + 1 early-return branch. All three must be deleted in the same commit.

## 7. Gate suite notes

### BEFORE D6
9 gates including 2 acceptance tests.

### AFTER D6
7 gates:
1. `cargo fmt --check`
2. `cargo clippy --all-targets -- -D warnings`
3. `cargo clippy --all-targets --features threads -- -D warnings`
4. `cargo test --lib`
5. `cargo test --lib --features threads`
6. wasm-pack build
7. `pnpm typecheck` / `pnpm build`

## 8. Acceptance check

1. `ls src/snapshot_hash.rs` → does not exist.
2. `ls tests/` → empty.
3. `grep -rn "snapshot_hash\|force_sequential_nn\|ACCEPTANCE_TICKS\|EVOSIM_WRITE_GOLDEN" src/ tests/ .github/` → zero (excluding docs/).
4. `cargo build --release` + `--features threads` succeeds.
5. `cargo test --lib` count drops by ~6.
6. `cargo test --lib --features threads` count drops by ~8.
7. clippy clean both feature flags.
8. wasm-pack build succeeds.

## 9. Open questions for the orchestrator

1. **Ordering vs D1** — Recommend D6 BEFORE D1.
2. **Visibility narrowing of `pub mod world`** — Recommend only remove `pub mod snapshot_hash;`. Conservative.
3. **Comment stripping aggressiveness** — Strip only factually-wrong, keep historical markers.
4. **BUILD-REPORT.md** — Strike the F.30/acceptance claim now (cheap insurance).
5. **`SimRng::state()` gating** — Recommend `#[cfg(test)]`.
6. **`tests/` empty directory** — No action needed (git ignores empty).
7. **New CI threaded gates** — D6 owns adding them.
8. **`#[cfg(not(feature = "threads"))]` gates** — Confirmed clean.

### Critical Files for Implementation
- /home/adamg/evosim/src/snapshot_hash.rs
- /home/adamg/evosim/tests/acceptance.rs
- /home/adamg/evosim/src/world/nn.rs
- /home/adamg/evosim/src/world/mod.rs
- /home/adamg/evosim/.github/workflows/ci.yml
